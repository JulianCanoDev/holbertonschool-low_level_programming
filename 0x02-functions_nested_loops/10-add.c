#include "holberton.h"

/**
 * add - Entry point
 * @num_one: int variable
 * @num_two: int variable
 * Return: Always 0 (Success)
 */

int add(int num_one, int num_two)
{
	int result;
	
	result = num_one + num_two;
	return (result);
}
