#include <stdio.h>
#include <stdlib.h>
#include <ctype.c>

/**
 * main - Entry point
 * @argc: int variable
 * @argv: char array pointer
 * Return: Always 0.
 */
int main(int argc, char *argv[])
{
	if (argc != 2)
	{
		printf("Error\n");
		return (1);
	}
}
